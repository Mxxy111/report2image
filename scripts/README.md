# Scripts

These scripts are secondary research utilities. The main outpatient app starts from
`run_web.py` or `python -m webapp.main`.

- `questions/`: generate, verify, and export patient-comprehension questions.
- `datasets/`: legacy retrospective sampling utilities, now pointed at `data/`.
- `analysis/`: statistical analysis templates, expecting CSVs under `data/analysis/`.

